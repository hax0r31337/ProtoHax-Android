<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/copy_logs"
        android:icon="@drawable/ic_baseline_content_copy_24"
        android:title="@string/logcat_copy"
        android:contentDescription="@string/logcat_copy"
        app:showAsAction="ifRoom"
        android:onClick="copyLogs" />

    <item
        android:id="@+id/clear_logs"
        android:icon="@drawable/ic_baseline_delete_24"
        android:title="@string/logcat_clear"
        android:contentDescription="@string/logcat_clear"
        app:showAsAction="ifRoom"
        android:onClick="clearLogs" />

</menu>